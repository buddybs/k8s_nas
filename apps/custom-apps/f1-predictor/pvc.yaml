apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: f1-predictor-data
  namespace: custom-apps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
